## 1. 线上全量数据采集与对比

- [x] 1.1 使用 Playwright 在 `https://v.flomoapp.com/mine` 检查分页接口和 IndexedDB 缓存，确认全量备忘录数量。
- [x] 1.2 从线上页面 IndexedDB 去重导出全部备忘录、标签、统计、时间和排序，记录最终采集数量。
- [x] 1.3 使用 Playwright 在 `http://localhost:8080/` 采集同一 viewport 下的文本、截图和关键数据。
- [x] 1.4 整理线上全量采集数据与本地数据的统计、标签、备忘录内容、排序和交互差异。

## 2. 数据与领域实现

- [x] 2.1 先补充后端默认数据、分页全量数据数量和 CRUD 期望的失败测试。
- [x] 2.2 更新 FastAPI 默认种子数据，确保后端返回线上分页采集出的全部数据。
- [x] 2.3 更新前端数据读取、统计和标签展示，确保 UI 不维护独立默认数据。

## 3. CRUD 与 UI 验证

- [x] 3.1 补充或更新前端单元测试，验证可见数据、统计和标签展示。
- [x] 3.2 补充或更新 Playwright E2E，验证创建、读取、更新、删除完整闭环。
- [x] 3.3 使用 Playwright 复查线上页面和本地页面截图，确认主要 UI/UX 与数据一致。

## 4. 仓库交付

- [x] 4.1 运行后端单元测试、前端单元测试、E2E 测试和 Docker Compose 启动验证。
- [x] 4.2 运行 OpenSpec 校验并确认 tasks 全部完成。
- [x] 4.3 提交包含 OpenSpec artifact、代码、测试和文档的 commit，创建 PR 并等待 GitHub Actions。
